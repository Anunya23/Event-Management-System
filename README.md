# Event-Management-System
Event Scheduling System is a C-based application that manages events using a priority-based linked list. Users can add, view, display, and delete events. Each event stores its name, date, time, and priority. Events are automatically arranged based on priority, making it easy to manage and access the most important upcoming events efficiently.
